<!DOCTYPE html>
<html lang="{{ grav.language.getActive ?: 'it' }}">
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <meta charset="utf-8" />
    <title>
      {% if header.title %}{{ header.title }} | {% endif %}{{ site.title | e }}
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=DM+Sans:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="{{ url('theme://css/theme.css') }}" />

    {# Optional: place for extra head content #} {% block head_extra %}{%
    endblock %}
  </head>

  <body class="min-h-screen flex flex-col bg-neutral-100 text-neutral-900">
    {% include 'partials/header.html.twig' %}

    <main class="flex-1">{% block content %}{% endblock %}</main>

    {% include 'partials/footer.html.twig' %}

    <script src="{{ url('theme://js/main.js') }}"></script>
    {% block body_extra %}{% endblock %}
  </body>
</html>
